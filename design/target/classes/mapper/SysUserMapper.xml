<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.term.design.dao.SysUserDao">
    <resultMap id="userMap" type="com.term.design.entity.SysUser">
    <id property="id" column="id"/>
    <result property="username" column="username"/>
    <result property="password" column="password"/>
    <collection property="roles" ofType="com.term.design.entity.SysRole">
        <result column="name" property="name"/>
    </collection>
    </resultMap>
    <select id="findByName" parameterType="String" resultMap="userMap">
		select u.*
		,r.name
		from sys_user u
        LEFT JOIN sys_user_role sru on u.id= sru.sys_user_id
        LEFT JOIN sys_role r on sru.roles_id=r.id
        where username= #{username}
	</select>

    <select id="getById" resultType="com.term.design.entity.SysUser">
        select *
        from sys_user
        where id=#{id}
    </select>
</mapper>